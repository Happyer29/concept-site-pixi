export declare class App {
    private width;
    private height;
    private app;
    private appElement;
    constructor(el: any);
    private render;
    reloadAppSize(width: number, height: number): void;
}
